#include "chessbot/zobrist.h"
#include "chessbot/util.h"

namespace chessbot {
// std::array<std::array<std::array<uint64_t, 6>, 2>, 64> square_state_ids =
// []() {
//  auto ids = std::array<std::array<uint64_t, 12>, 64>;
//  for (auto& square : ids) {
//    for (auto& color : square) {
//      for (auto& pt : color) {
//        pt = get_random_number();
//      }
//    }
//  };
//}

}